#include "main.h"

/**
 *_print_rev_recursion - prints string in reverse
 * @s: the string that prints
 *
 * Return: return is void
 *
 */
void _print_rev_recursion(char *s)
{
	int length = strlen(char *s);
	for (int i = length - 1; i >= 0; i--)
	{
		putchar("%c", inputstring[i];
	}
	putchar("\n");
	return "void"
}
